# ── API ───────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Audio I/O (required by every provider) ───────────────────────────
numpy==1.26.4
soundfile==0.12.1
resampy==0.4.3

# ── Primary engine: GOPT (multi-aspect, multi-granularity) ────────────
# Install GOPT + Montreal Forced Aligner per their READMEs:
#   pip install montreal-forced-aligner
#   git clone https://github.com/YuanGongND/gopt && cd gopt && pip install -e .
# Then set GOPT_MODEL_PATH to a downloaded checkpoint.

# ── Local fallback (self-hosted, CPU-only): faster-whisper ASR ───────
# pip install faster-whisper

# ── Tests ─────────────────────────────────────────────────────────────
pytest==8.3.2
httpx==0.27.0
